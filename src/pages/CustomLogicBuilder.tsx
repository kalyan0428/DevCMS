import { classNames } from "./landingPage/classess";
const CustomLogicBuilder = () => {
  const { pageHeader } = classNames;
  return (
    <div>
      <h3 className={pageHeader}>CustomLogic Builder</h3>
    </div>
  );
};

export default CustomLogicBuilder;
