import { classNames } from "./landingPage/classess";
const HistoricalProcessViewer  = () => {
  const { pageHeader } = classNames;
  return (
    <div>
      <h3 className={pageHeader}>HistoricalProcess Viewer</h3>
    </div>
  );
};

export default HistoricalProcessViewer;
