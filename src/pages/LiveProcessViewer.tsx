import { classNames } from "./landingPage/classess";
const LiveProcessViewer = () => {
  const { pageHeader } = classNames;
  return (
    <div>
      <h3 className={pageHeader}>LiveProcess Viewer</h3>
    </div>
  );
};

export default LiveProcessViewer;
