import { classNames } from "./landingPage/classess";
const AlarmOverview = () => {
  const { pageHeader } = classNames;
  return (
    <div>
      <h3 className={pageHeader}>Alarm Overview</h3>
    </div>
  );
};

export default AlarmOverview;
