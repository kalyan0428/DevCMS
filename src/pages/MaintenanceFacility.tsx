import { classNames } from "./landingPage/classess";
const MaintenanceFacility = () => {
  const { pageHeader } = classNames;
  return (
    <div>
      <h3 className={pageHeader}>Maintenance Facility</h3>
    </div>
  );
};

export default MaintenanceFacility;
